mod compiler;
mod interpreter;
mod parser;
mod types;
mod utils;

fn main() {
    println!("Language Interpreter/Compiler 시작");
    // 여기에서 언어 인터프리터 또는 컴파일러 로직을 시작합니다.
    // 예: 파일 읽기, 소스 코드 파싱, 실행 등
}
